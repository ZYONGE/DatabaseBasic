# 260916

## Architecture
---

### ANSI / SPARC

 - ANSI: American National Standards Institute (미국 국가 표준 협회)
 - SPARC: Standards Planning And Requirements Committee (표준 계획 요구 위원회)
 - 데이터베이스를 3단계로 나눈 구조를 제안 -> 3단계 데이터베이스 구조

 - 외부 단계: 각 사용자의 뷰
   - 사용자마다 필요한 부분만 정의한 스키마 (외부 스키마 / 서브 스키마)
   - 사용자마다 다르므로 여러 개 존재
 - 개념 단계: 사용자 공동체의 뷰
   - 조직 전체의 데이터베이스를 정의한 스키마 (개념 스키마)
   - 엔티티, 관계, 제약조건, 보안을 명시
   - 데이터베이스마다 하나만 존재
 - 내부 단계: 물리적 또는 저장 뷰
   - 실제 저장 구조를 정의한 스키마 (내부 스키마)
   - 저장 레코드의 배치, 인덱스, 접근 경로
   - 데이터베이스마다 하나만 존재

---

### 스키마 간 사상 (External - Conceptual - Internal)

 - 사상(mapping): 단계가 다른 스키마끼리 대응 관계를 잡아주는 것

 - 외부 사상 -> 개념 사상 (외부/개념 사상)
   - 외부 단계의 뷰 -> 사용자의 질의
   - 그 질의를 개념 단계의 스키마를 사용한 질의로 변환

 - 개념 사상 -> 내부 사상 (개념/내부 사상)
   - 내부 단계의 스키마로 변환
   - 디스크의 데이터베이스로 접근

---

### 데이터 독립성

 - 논리적 데이터 독립성
   - 개념 스키마가 바뀌어도 외부 스키마와 응용 프로그램은 영향을 받지 않음
   - 외부/개념 사상만 고쳐주면 됨
 - 물리적 데이터 독립성
   - 내부 스키마(저장 구조, 인덱스)가 바뀌어도 개념 스키마는 영향을 받지 않음
   - 개념/내부 사상만 고쳐주면 됨
 - 논리적 독립성이 물리적 독립성보다 달성하기 어려움
 - 아키텍처 층위: 사용자 - 뷰 - 개념 스키마 - 내부 스키마

---

### DBMS Architecture 명세

 - 최상위: 데이터베이스 관리자 / 사용자
 - 2상위: 데이터베이스 정의 / 기작성 트랜잭션 & 쿼리
 - 3상위: 데이터 정의어 컴파일러 / 런타임 관리기 / 질의 처리기(런타임 관리기에 예속됨)
 - 4상위: SSD

 - 데이터 정의어 컴파일러 모듈 (DDL 모듈): 테이블 생성 요청에 의해 파일 형태로 테이블을 생성, 명세를 시스템 카탈로그에 저장
 - 질의 처리기 모듈 (Query Engine Module): 질의 처리 요청에 의해 처리하는 모듈
 - 런타임 관리기 (Runtime Manager): 실행 중인 질의와 트랜잭션이 실제로 저장 데이터에 접근하도록 관리
 - ODBC API: DBMS 간의 서로 상대방의 데이터베이스를 접근할 수 있음
   - Open DataBase Connectivity, DBMS 종류에 상관없이 같은 방식으로 접근하게 해주는 표준 인터페이스

---

### DBMS 종류

 - 중앙 집중식 데이터베이스 시스템: DBMS가 하나의 컴퓨터 시스템 위에서 운영됨

 - 분산 데이터베이스 시스템
   - 네트워크로 연결된 여러 사이트에 DBMS가 분산되어 있음
   - 여러 컴퓨터 시스템에서 운영됨
   - 사용자는 다른 사이트에 저장된 데이터베이스에도 접근할 수 있음

 - 클라이언트 - 서버 데이터베이스 시스템
   - 자체 컴퓨팅 능력을 가진 클라이언트를 통해서 서버에 접근
   - 하나의 데이터베이스 서버에 저장
   - 기능이 서버와 클라이언트에 분산됨
   - 서버는 DB를 저장, 질의 최적화, 권한 검사, 동시성 제어와 회복, 무결성 유지 등 대부분 기능을 수행
   - 클라이언트는 사용자 인터페이스를 관리하고 응용을 수행

   - 3층 모델 (3-tier model)
     - 응용 서버를 통해 클라이언트와 DB를 연결
     - 클라이언트 - 응용 서버 - 데이터베이스 서버
     - 응용 서버가 중간에서 업무 로직을 처리하고 DB 접근을 대신함

   - 데이터베이스를 보다 넓은 지역에서 접근 가능
   - 다양한 컴퓨터 시스템 사용 가능

   - 다만 보안이 취약함
     - 클라이언트가 DB 서버에 직접 접근하므로 노출 지점이 많아짐
     - 3층 모델은 응용 서버가 DB를 가려주므로 이 점이 보완됨

---

## 관계 데이터 모델과 제약조건
---

### 관계 데이터 모델

 - 테이블을 사용
 - 효율적인 설계, 질의문 사용
 - 릴레이션의 관점에서 모든 데이터를 논리적으로 구성
 - 응용 프로그램들은 DB 내부 레코드들의 어떠한 순서와도 무관하게 작성
 - 사용자는 데이터만 명시, 어떻게 찾을 것인가는 명시할 필요가 없음
 - 연관된 데이터를 연결하기 위해 링크나 포인터를 사용하지 않음

 - 릴레이션 (Relation): 행과 열로 이루어진 하나의 테이블
 - 레코드 (Record): 릴레이션의 한 행. 파일 시스템에서 쓰던 이름이고 관계 모델에서는 튜플이라고 부름
 - 튜플 (Tuple): 릴레이션의 한 행. 개체 하나에 대한 정보
 - 어트리뷰트 (Attribute): 릴레이션의 한 열. 개체가 가지는 속성

 - 용어 대응

| 관계 데이터 모델 | 파일 시스템 |
| --- | --- |
| 릴레이션 | 파일 |
| 튜플 | 레코드 |
| 어트리뷰트 | 필드 |

---

### 릴레이션의 형태

 - 학생 릴레이션

| 학번 | 이름 | 학과 | 학년 |
| --- | --- | --- | --- |
| 23001 | 김철수 | 컴퓨터공학과 | 2 |
| 23002 | 이영희 | 소프트웨어학과 | 1 |
| 23003 | 박민수 | 컴퓨터공학과 | 3 |

 - 표 전체: 릴레이션 (학생)
 - 열의 이름 (학번, 이름, 학과, 학년): 어트리뷰트
 - 한 행 (23001, 김철수, 컴퓨터공학과, 2): 튜플
 - 차수 (Degree): 어트리뷰트의 개수 -> 4
 - 카디널리티 (Cardinality): 튜플의 개수 -> 3
 - 도메인 (Domain): 각 어트리뷰트가 가질 수 있는 값의 집합
   - 예) 학년의 도메인은 1 ~ 4 의 정수
